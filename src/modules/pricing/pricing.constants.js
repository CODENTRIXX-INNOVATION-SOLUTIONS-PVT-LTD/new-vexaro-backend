'use strict';

const { SystemConfig } = require('../../constants');

module.exports = {
  VOLUMETRIC_DIVISOR: SystemConfig.VOLUMETRIC_DIVISOR,
  RTO_CHARGE_DEFAULT: SystemConfig.RTO_CHARGE_DEFAULT,
  DEFAULT_SUPER_ADMIN_MARKUP: SystemConfig.DEFAULT_SUPER_ADMIN_MARKUP,
};
